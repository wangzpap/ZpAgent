# ============================================
# ZpAgent 后端依赖
# ============================================

# Web 框架
fastapi>=0.137.0,<1.0
# 注意: mcp 1.13+ 要求 uvicorn>=0.31.1，若低于此版本会导致全新安装无法解析依赖
uvicorn[standard]==0.31.1

# LangChain 生态（提供 @tool 装饰器、create_agent 等）
langchain>=1.2.0,<2.0
langchain-openai>=1.2.0,<2.0

# LangGraph（ReAct Agent 图引擎）
langgraph>=1.2.0,<2.0

# MCP（Model Context Protocol）工具适配
# 注意: 若低于以下版本会出现组合不兼容——
#   - langchain-mcp-adapters 0.3.0 依赖 mcp 1.12.x（streamable_http_client 已被移除）
#     导致 ImportError，需 >=0.3.2
#   - mcp 1.13+ 要求 uvicorn>=0.31.1（已在上方相应调整）
# 固定 mcp <2.0 防止新版破坏性变更
langchain-mcp-adapters>=0.3.2,<1.0
mcp>=1.29.0,<2.0

# 数据校验 & 配置管理
pydantic>=2.9.0,<3.0
pydantic-settings>=2.5.0,<3.0

# MySQL 异步驱动（会话元数据 + 对话检查点持久化存储后端共用）
aiomysql>=0.2.0,<1.0
langgraph-checkpoint-mysql>=2.0.0,<4.0
